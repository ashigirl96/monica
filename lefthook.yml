pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,tsx,js,jsx}"
      run: bunx oxlint
    fmt-check:
      glob: "*.{ts,tsx,js,jsx}"
      run: bunx oxfmt --check

pre-push:
  commands:
    clippy:
      run: cargo clippy --workspace --all-targets -- -D warnings
