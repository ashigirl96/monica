pre-commit:
  parallel: true
  commands:
    lint:
      run: bunx oxlint
    fmt-check:
      run: bunx oxfmt --check

pre-push:
  commands:
    db-validate:
      run: just db-validate
    clippy:
      run: cargo clippy --workspace --all-targets -- -D warnings
