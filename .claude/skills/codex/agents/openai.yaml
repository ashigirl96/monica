interface:
  display_name: "Codex CLI"
  short_description: "Run Codex CLI with structured prompts for design/review"
  default_prompt: "Use codex CLI with a structured prompt and return concise actionable output."
